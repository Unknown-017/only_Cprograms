Q 9.Enter a number and check whether it is greater than or less than or
    equal to 10. 

=>

#include <stdio.h>
int main()
{
    int a;
    printf("Enter a number: ");
    scanf("%d", &a);
    if (a == 10)
    {
        printf("%d is equal to 10", a);
    }
    else if (a > 10)
    {
        printf("%d is greater than 10", a);
    }
    else
    {
        printf("%d is less than 10", a);
    }
    return 0;
}
